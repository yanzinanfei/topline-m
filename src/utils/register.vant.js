// 封装vant组件
import Vue from 'vue'

import {
  Button,
  Cell,
  CellGroup,
  Icon,
  NavBar,
  Field,
  Toast
} from 'vant'

Vue.use(Button)
  .use(Cell)
  .use(CellGroup)
  .use(Icon)
  .use(NavBar)
  .use(Field)
  .use(Toast)
